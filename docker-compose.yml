# Spin up commands based on STORAGE_ADAPTER in src/config/identifiers.ts:
#   postgres (default): docker compose up
#   clickhouse:         docker compose --profile clickhouse up
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: scrawn
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./scrawn.init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  clickhouse:
    profiles:
      - clickhouse
    image: clickhouse/clickhouse-server:latest
    restart: always
    ports:
      - "8123:8123"
      - "9000:9000"
    environment:
      CLICKHOUSE_USER: default
      CLICKHOUSE_PASSWORD: clickhouse
      CLICKHOUSE_DB: scrawn
    volumes:
      - chdata:/var/lib/clickhouse
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8123/ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  server:
    image: scrawndotdev/scrawn:latest
    restart: unless-stopped
    ports:
      - "8070:8070"
      - "8069:8069"
    depends_on:
      db:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://postgres:postgres@db:5432/scrawn
      CLICKHOUSE_URL: http://default:clickhouse@clickhouse:8123/scrawn
      NODE_ENV: production
      HMAC_SECRET: ${HMAC_SECRET}

  dashboard:
    image: scrawndotdev/dashboard:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://postgres:postgres@db:5432/dashboard
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
      BETTER_AUTH_URL: http://localhost:3000
      SCRAWN_BASE_URL: http://server:8069
      SCRAWN_KEY: ${SCRAWN_KEY}

volumes:
  pgdata:
  chdata:
